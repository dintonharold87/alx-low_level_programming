#include "main.h"
/**
 * mul - multiply to integers
 * @a : first integer
 * @b : second integer
 * Return: the product after multiplying two integers.
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
